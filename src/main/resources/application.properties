# Application Information
spring.application.name=WebPet

# Database Configuration - Configurado para funcionar com Docker
# Para ambiente local, modificar para localhost:5433
spring.datasource.url=jdbc:postgresql://db:5432/webpet_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security Configuration 
# TODO: Em produção, utilizar variáveis de ambiente para secrets (SPRING_SECURITY_TOKEN_SECRET)
# e nunca armazenar senhas em texto plano em arquivos de configuração
api.security.token.secret=macaco-branco-com-tenis-da-nike